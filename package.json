{
  "name": "its-you",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -w -p .",
    "start": "ts-node src/app.ts",
    "dev": "ts-node-dev --respawn --transpile-only --notify src/app.ts"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.1"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
